name: CI

on: [push, pull_request]

env:
  PACKAGE_NAME: 'oxid-professional-services/easycredit-module'
  MODULE_PATH: 'vendor/oxid-professional-services/easycredit-module'
  MODULE_TESTS_PATH: 'vendor/oxid-professional-services/easycredit-module/tests'
  FULL_MODULE_PATH: '/tmp/oxid7/vendor/oxid-professional-services/easycredit-module'
  MODULE_BRANCH: 'dev-main'
  OXID_PATH: '/tmp/oxid7'

jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      max-parallel: 15
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest]
        # oxid-versions: ['dev-b-7.0-ce', 'dev-b-7.1-ce', 'dev-b-7.2-ce', 'dev-b-7.3-ce', 'dev-b-7.4-ce', 'dev-b-7.5-ce']
        # oxid-versions: ['7.5']
        oxid-versions: ['dev-b-7.5-ce']
        # php-versions: ['8.0', '8.1', '8.2', '8.3', '8.4', '8.5']
        php-versions: ['8.3']
        exclude:
          - oxid-versions: '7.0'
            php-versions: '8.2'
          - oxid-versions: '7.0'
            php-versions: '8.3'
          - oxid-versions: '7.5'
            php-versions: '8.1'            
          - oxid-versions: '7.5'
            php-versions: '8.2'            
    name: Oxid ${{ matrix.oxid-versions }} on PHP ${{ matrix.php-versions }}
    steps:
#      - name: Checkout
#        uses: actions/checkout@master

#      - name: Install PHP
#        uses: shivammathur/setup-php@master
#        with:
#          php-version: ${{ matrix.php-versions }}
#          extensions: mbstring, xdebug, gd, bcmath, ctype, curl, dom, hash, iconv, intl, openssl, simplexml, soap, xsl, zip
#          tools: composer:v2

#      - name: Check PHP Version
#        run: php -v

#      - name: Check Composer Version
#        run: composer -V
        
      - name: Cleanup workspace
        run: |
          ls -la ./
          sudo rm -rf ./* || true
          sudo rm -rf ./.??* || true
          ls -la ./        
        
      - name: Clone testing environment
        run: git clone https://github.com/OXID-eSales/docker-eshop-sdk.git .

      - name: Clone the shop
        # run: git clone --depth 1 https://github.com/OXID-eSales/oxideshop_ce.git --branch b-${{ matrix.oxid-versions }}.x --single-branch source
        run: |
          composer create-project -n --no-progress --no-install oxid-esales/oxideshop-project ./source ${{ matrix.oxid-versions }}
          cd source
          composer install --no-progress --no-suggest --no-interaction
          
        
      - name: Prepare container configuration
        run: |
          make setup
          make addbasicservices
          make file=services/selenium-chrome.yml addservice
          perl -pi\
            -e "s#PHP_VERSION=.*#PHP_VERSION=${{ matrix.php-versions }}#g;"\
            .env
          perl -pi\
            -e "s#MYSQL_VERSION=.*#MYSQL_VERSION=5.7#g;"\
            .env
          perl -pi\
            -e 's#display_errors =.*#display_errors = false#g;'\
            -e 'print "xdebug.max_nesting_level=1000\nxdebug.mode=coverage\n\n"'\
            containers/php/custom.ini

          perl -pi\
            -e 's#/var/www/#/var/www/source/#g;'\
            containers/httpd/project.conf

      - name: Prepare shop configuration
        run: |
          cp source/source/config.inc.php.dist source/source/config.inc.php
          sed -i "1s+^+SetEnvIf Authorization "\(.*\)" HTTP_AUTHORIZATION=\$1\n\n+" source/source/.htaccess

      - name: Start containers
        run: |
          make up
          sleep 5
                  
      - name: Checkout current module
        uses: actions/checkout@v5
        with:
          path: source/extensions/easycredit

      - name: Install module
        run: |
          docker compose exec -T \
            php composer config repositories.oxid-professional-services/easycredit-module \
            --json '{"type":"path", "url":"/var/www/extensions/easycredit", "options": {"symlink": true}}'
          docker compose exec -T \
            php composer config audit.block-insecure false
          docker compose exec -T php composer require oxid-professional-services/easycredit-module:*  --no-update
          docker compose exec -T php composer dump
          # docker compose exec -T php composer install --no-progress --no-suggest --no-interaction
          docker compose exec -T php cat composer.json
          
      - name: Setup Shop and demodata
        run: |
          docker compose exec -T php /var/www/vendor/bin/oe-console oe:setup:shop \
            --db-host mysql \
            --db-port 3306 \
            --db-name example \
            --db-user root \
            --db-password root \
            --shop-url http://localhost.local \
            --shop-directory /var/www/source \
            --compile-directory /var/www/source/tmp
          
      - name: Install Apex Theme
        run: |
          docker compose exec -T php /var/www/vendor/bin/oe-console oe:theme:activate apex

      - name: set composer.json to dev stability
        run: |
          docker compose exec -T \
          php sed -i 's/"minimum-stability": "stable",/"minimum-stability": "dev",/' /var/www/composer.json

      - name: Install Testing Lib
        run: |          
          docker compose exec -T \
            php composer require oxid-esales/testing-library:dev-b-8.0.x
          

      - name: Activate Easycredit Module
        run: |
          docker compose exec -T php  ls -la /var/www
          docker compose exec -T php  ls -la /var/www/source
          docker compose exec -T php  ls -la /var/www/extensions/easycredit
          docker compose exec -T php /var/www/vendor/bin/oe-console oe:module:install /var/www/extensions/easycredit 
          docker compose exec -T php /var/www/vendor/bin/oe-console oe:module:activate osceasycredit
          docker compose exec -T php /var/www/vendor/bin/oe-console oe:cache:clear
          
      - name: Put module settings
        continue-on-error: true
        run: |
          docker compose exec -T php mkdir -p /var/www/var/configuration/shops/1/modules/osceasycredit.yaml
          docker compose exec -T php echo "${{ secrets.MODULE_SETTINGS }}" > /var/www/var/configuration/shops/1/modules/osceasycredit.yaml
          docker compose exec -T php cat /var/www/var/configuration/shops/1/modules/osceasycredit.yaml          
          
      - name: Run tests
        run: |
          docker compose exec -T \
           -e XDEBUG_MODE=coverage \
           php vendor/bin/phpunit \
             -c /var/www/extensions/easycredit/tests/phpunit.xml \
             --testsuite=Integration \
             --bootstrap=/var/www/source/bootstrap.php \
             var/www/extensions/easycredit/tests/tests | tee integration_PHP${{ matrix.php }}_MYSQL${{ matrix.mysql }}_phpunit_log.txt 2>&1 || true
          if grep -q -Ei 'fail|\\.\\=\\=|Warning|Notice|Deprecated|Fatal|Error' integration_PHP${{ matrix.php }}_MYSQL${{ matrix.mysql }}_phpunit_log.txt; then 
            exit 1; 
          fi          
        

          